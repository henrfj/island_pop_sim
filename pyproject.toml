[project]
name = "island-pop-sim"
version = "0.1.0"
description = "Stochastic island population-genetics simulation"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0"]

[tool.uv]
package = false